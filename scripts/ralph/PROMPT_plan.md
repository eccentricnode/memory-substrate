0a. Study `specs/*` with up to 250 parallel subagents to learn the application specifications.
0b. Study `@IMPLEMENTATION_PLAN.md` (if present) to understand the plan so far.
0c. Study `reference/*` (the shared, substrate-agnostic library — validator, compactor, migrator) and `@SPEC.md` (the memory-substrate contract this adapter must satisfy) to understand shared utilities & the conformance surface.
0d. For reference, the application source code is in `adapters/pi-dev/extension/*`. The host is pi.dev (pi-coding-agent). Treat `@SPEC.md` and `@STRATEGY.md` as authoritative for behavior.

1. Study `@IMPLEMENTATION_PLAN.md` (if present; it may be incorrect) and use up to 500 subagents to study existing source code in `src/*` and compare it against `specs/*`. Use a high-reasoning subagent to analyze findings, prioritize tasks, and create/update `@IMPLEMENTATION_PLAN.md` as a bullet-point list sorted by priority of items yet to be implemented. Ultrathink. Consider searching for TODOs, minimal implementations, placeholders, skipped/flaky tests, and inconsistent patterns. Study `@IMPLEMENTATION_PLAN.md` to determine starting point for research and keep it up to date with items considered complete/incomplete using subagents.

IMPORTANT: Plan only. Do NOT implement anything. Do NOT assume functionality is missing; confirm with code search first. Treat `src/lib` as the project's standard library for shared utilities and components. Prefer consolidated, idiomatic implementations there over ad-hoc copies.

ULTIMATE GOAL: Build and verify an event-driven pi.dev memory-substrate extension (the pi-dev adapter's §6.2 optional forced-write capability) that observes session events, debounces/batches them, runs a cheap background pi subprocess to decide whether anything is durable, writes spec-conformant memories (SPEC §2–§5) only to the resolved memory root, honors disabled mode (§4.5) and ignore mode (§4.4), guards against recursion and out-of-root writes, ships a dry-run mode, and injects only small relevant memory snippets at session start. Consider missing elements and plan accordingly. If an element is missing, search first to confirm it doesn't exist, then if needed author the specification at `specs/NN-FILENAME.md`. If you create a new element, document the plan to implement it in `@IMPLEMENTATION_PLAN.md` using a subagent.
