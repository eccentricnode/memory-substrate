{
  "name": "memory-substrate",
  "version": "0.1.0-draft",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "bun test",
    "test:pi-live": "PI_MEMORY_INTEGRATION=1 bun test tests/pi-dev-live-integration.test.ts",
    "typecheck": "bunx tsc --noEmit",
    "validate": "bun reference/validator.ts",
    "compact": "bun reference/compactor.ts",
    "migrate": "bun reference/migrator.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.2.22",
    "typescript": "^5.9.3"
  }
}
